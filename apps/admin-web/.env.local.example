# URL de l'API NestJS (apps/api) — doit correspondre à ADMIN_WEB_ORIGIN côté API pour le CORS.
NEXT_PUBLIC_API_URL=http://localhost:3000

# Optionnel — affiche un lien "Métriques (Grafana)" dans la barre latérale si défini.
NEXT_PUBLIC_GRAFANA_URL=http://localhost:3002
